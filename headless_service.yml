apiVersion: v1
kind: Service
metadata:
  name: nginx           # ❗ This name MUST match StatefulSet.spec.serviceName
spec:
  clusterIP: None       # ❗ Headless Service → required for StatefulSet
  selector:
    app: nginx          # Must match pod labels
  ports:
  - port: 80
