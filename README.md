First we need to create the haedless service used by statefulsets to provide stable DNS for each pod,,,so first create the service & then the statefulset,,,the service name should
match the statefulsets service name.

